# For loop
 for (var in 1:10) {
   print(paste("number:", var))
   #past() function can be used for concatation of the string with variable
 }
 
 v<-c("virat", "Rohit", "kurnal", "hardik", "surya")
 for (val in v) {
   print(v)
   
 }
 
 # repate loop:
 x<-1
 repeat{
   print("yash")
   x=x+1
   if(x==6){
     break
   }
 }
 
 #While Loop:
 # In while loop, Every thing you have to initilize before while loop
 vec= c("idnd" , "evdvr", "fdvr", "trre")
 x<-1
 while(x<=5){
   print(vec)
   x<-x+1
 }
 